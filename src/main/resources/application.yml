# security 使能
#security.basic.enabled = false
spring:
  datasource:
    username: root
    password: wjk123456
    url: jdbc:mysql://127.0.0.1:3306/xuexiao?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai

  thymeleaf:
    enabled: true
    cache: false #禁用缓存
    prefix: classpath:/templates/
    suffix: .html
    encoding: utf-8
    mode: html
  mvc:
    static-path-pattern: /static/**
mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
# 打印SQL语句
logging:
  level:
    com.leien.dao : debug

pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql

server:
  port: 8882
